import pytest
import json

def pytest_json_report(rawdict):
    """
    This hook modifies the raw dictionary of the test report generated by pytest.
    We're adding tool name (or other metadata) to the report here.
    """
    tool_name = "example_tool"  # Set the tool name here, you can fetch dynamically if needed
    rawdict['tool_name'] = tool_name  # Add tool name to the report metadata

    # Optionally, modify other parts of the report (e.g., add versions, status, etc.)
    return rawdict
